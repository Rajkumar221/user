pipeline {
    agent { node { label 'workstation' } }

    stages{
      stage { 'Build' }{
        steps {
            sh 'npm install'
        }
      }

      stage { 'Unit tests' }{
        steps {
            echo 'Unit tests'
        }
      }

      stage { 'code Analysis' }{
        steps {
            echo 'code Analysis'
        }
      }

      stage { 'security Scans' }{
        steps {
            echo 'security Scans'
        }
      }

      stage { 'Publish a Artifact' }{
        steps {
            echo 'Publish a Artifact'
        }
      }

    }
}
